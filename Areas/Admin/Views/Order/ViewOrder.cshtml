@using QLDienThoai.Models
@model IEnumerable<OrderDetails>
@{
    ViewData["title"] = "OrderDetail Lists";
}

<h1> OrdersDetail</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Order Code</th>
            <th scope="col">UserName</th>

            <th scope="col">Price</th>
            <th scope="col">Quantity</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th></th>
                <th scope="row">@item.OrderCode</th>
                <td>@item.Username</td>

                <td>@item.Price</td>
                <td>@item.Quantity</td>
                
               @*  <td>
                    <a asp-action="ViewOrder" class="btn btn-warning btn-sm" asp-controller="Order" asp-route-IdOrder="@item.OrderCode">View Order</a>
                    <a asp-action="Delete" class="btn btn-warning btn-sm" asp-controller="Order" asp-route-IdOrder="@item.OrderId">Delete</a>
                </td> *@

            </tr>
        }
    </tbody>
</table>